# Domain for TLS certificate and nginx vhost.
# Must match the DNS A record pointing to this server.
DOMAIN=your-domain.example.com

# Email for Let's Encrypt certificate notifications.
EMAIL=you@example.com
