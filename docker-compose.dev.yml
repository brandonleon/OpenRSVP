services:
  app:
    build:
      context: .
    container_name: openrsvp_app_dev
    ports:
      - "${OPENRSVP_HOST_PORT:-8000}:8000"
    volumes:
      - "${OPENRSVP_DATA_DIR:-./data}:/app/data"
    environment:
      UVICORN_RELOAD: "true"
    restart: unless-stopped
